# iris-streamlit-app
The Iris Flower Prediction App is a machine learning web application built using Python and Streamlit. It uses a Random Forest classifier trained on the Iris dataset to predict flower species based on sepal and petal measurements. The app allows user input through sliders and provides real-time predictions via a web interface.
